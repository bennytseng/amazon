<h1>All Products</h1>

<% @products.each do |p|  %>
  <div>
    <h2><%= link_to p.title, product_path(p) %></h2>
    <p><%= p.description %></p>
    <p> <%= number_to_currency(p.price) %></p>
    <hr>
  </div>
<% end %>
